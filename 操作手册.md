# QQ Bot 公开操作手册

这份手册面向公开仓库使用者，覆盖本项目的部署、NapCat 登录、配置文件修改、机器人管理命令、状态检查、排障和备份。文档中的账号、群号、服务器地址和密钥全部使用占位符，请按自己的环境填写。

本公开仓库不包含真实 `.env`、`config/config.json`、`config/persona_profile.local.json`、数据库、日志、二维码、QQ 登录态、API Key、Token、服务器密码或任何私有账号信息。

## 1. 目录和文件

公开仓库包含：

- `app/`：机器人核心代码。
- `bot.py`：NoneBot 入口。
- `config/config.example.json`：配置模板。
- `config/persona_profile.example.json`：角色画像模板。
- `.env.example`：环境变量模板。
- `tools/`：状态检查、备份、导出、部署等工具。
- `scripts/`：Windows 和 Linux 运维脚本模板。
- `操作手册.md`：当前手册。
- `README.md`：快速说明。

公开仓库不包含：

- `docs/` 设计文档和验收记录。
- `tests/` 测试代码。
- `.env`。
- `config/config.json`。
- `config/persona_profile.local.json`。
- `data/` 里的数据库和备份。
- `logs/`。
- `runtime_artifacts/`。
- NapCat 的登录态、缓存、二维码和截图。

## 2. 基础准备

### 2.1 Python 环境

建议 Python 版本为 `3.12` 或 `3.13`。

Windows：

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
pip install -e .[dev]
```

Linux：

```bash
python3.12 -m venv .venv
. .venv/bin/activate
python -m pip install -U pip
pip install -e .[dev]
```

如果不需要开发测试依赖，可以使用：

```bash
pip install -e .
```

### 2.2 准备私有环境变量

复制模板：

```powershell
Copy-Item .env.example .env
```

Linux：

```bash
cp .env.example .env
```

填写 `.env`：

```env
QQ_BOT_MODEL_API_KEY=
QQ_BOT_ONEBOT_TOKEN=
QQ_BOT_CONFIG_PATH=config/config.json
```

`QQ_BOT_ONEBOT_TOKEN` 只有在 NapCat/OneBot 配了访问令牌时才需要填写。不要提交 `.env`。

### 2.3 准备私有配置

复制配置模板：

```powershell
Copy-Item config\config.example.json config\config.json
Copy-Item config\persona_profile.example.json config\persona_profile.local.json
```

Linux：

```bash
cp config/config.example.json config/config.json
cp config/persona_profile.example.json config/persona_profile.local.json
```

`config/config.json` 是私有配置，必须按自己的 QQ、群、模型和端口填写。不要提交它。

## 3. 配置说明

### 3.1 `qq`

示例：

```json
{
  "selfId": "BOT_QQ",
  "rootUserIds": ["ROOT_QQ"],
  "ownerUserIds": ["OWNER_QQ"],
  "allowedPrivateUserIds": ["ROOT_QQ"],
  "allowedGroupIds": ["ALLOWED_GROUP_ID"],
  "groupMuteControllerUserIds": ["ROOT_QQ"],
  "memoryAllowedUserIds": ["ROOT_QQ"],
  "nicknames": ["测试号", "机器人"]
}
```

字段含义：

- `selfId`：机器人 QQ 号。
- `rootUserIds`：最高权限用户。root 自动拥有 owner 权限和私聊白名单权限。
- `ownerUserIds`：普通管理员，可使用 `/status`、`/allow ...` 等 owner 命令。
- `allowedPrivateUserIds`：允许普通私聊触发机器人的用户。
- `allowedGroupIds`：允许群聊触发机器人的群号。
- `groupMuteControllerUserIds`：允许在群里控制机器人静默/恢复的用户。
- `memoryAllowedUserIds`：允许写入长期记忆的用户。
- `nicknames`：群聊昵称触发词。

### 3.2 `onebot`

反向 WebSocket 常用配置：

```json
{
  "mode": "ws_reverse",
  "accessTokenEnv": "QQ_BOT_ONEBOT_TOKEN",
  "host": "127.0.0.1",
  "port": 8080,
  "apiRoot": "http://127.0.0.1:3000"
}
```

NapCat 需要把反向 WebSocket 地址配置成：

```text
ws://YOUR_SERVER_IP:8080/onebot/v11/ws
```

如果 NapCat 和 QQ_bot 在同一台机器上，也可以使用：

```text
ws://127.0.0.1:8080/onebot/v11/ws
```

### 3.3 `model`

本项目使用 OpenAI-compatible Chat Completions 请求。示例：

```json
{
  "provider": "openai-compatible",
  "apiKeyEnv": "QQ_BOT_MODEL_API_KEY",
  "baseUrl": "https://api.example.com/v1",
  "name": "your-model-name",
  "timeoutSeconds": 20,
  "temperature": 0.8,
  "maxTokens": 300
}
```

不要把真实 API Key 写入 JSON；只写环境变量名。

### 3.4 `persona`

```json
{
  "mode": "fixed_style_profile",
  "profilePath": "config/persona_profile.local.json",
  "fallbackProfilePath": "config/persona_profile.example.json"
}
```

`persona_profile.local.json` 是本地私有角色画像，公开仓库只保留示例模板。

## 4. 启动 QQ_bot

Windows：

```powershell
.\.venv\Scripts\Activate.ps1
python bot.py
```

Linux：

```bash
. .venv/bin/activate
python bot.py
```

启动后检查 `8080` 是否监听：

```bash
python tools/inspect_runtime_status.py --limit 5
```

关键字段：

```json
{
  "qq_bot_listening": true,
  "napcat_webui_listening": true,
  "napcat_to_bot_established": true
}
```

## 5. NapCat 登录与连接

### 5.1 启动 NapCat

NapCat 可以本机运行，也可以 Docker 运行。具体安装方式以 NapCat 官方文档为准。本项目只要求 NapCat 能通过 OneBot V11 反向 WebSocket 连到 QQ_bot。

### 5.2 配置反向 WS

在 NapCat OneBot 配置里填：

```text
ws://YOUR_SERVER_IP:8080/onebot/v11/ws
```

如果设置了 Access Token，请和 `.env` 里的 `QQ_BOT_ONEBOT_TOKEN` 保持一致。

### 5.3 扫码登录

在 NapCat WebUI 或日志里获取二维码，用机器人 QQ 扫码登录。二维码、缓存和登录态属于运行态文件，不要提交到 Git。

### 5.4 验证连接

运行：

```bash
python tools/inspect_runtime_status.py --limit 5
```

看到：

```json
"napcat_to_bot_established": true
```

就说明 NapCat 已连上 QQ_bot。

## 6. Owner 和 Root 命令

管理命令只在私聊生效。非 owner/root 发送管理命令会静默。

### 6.1 帮助

```text
/help
```

owner/root 可以用一条消息查看可用命令。root 会额外看到 `/owner ...` 命令。

### 6.2 状态和诊断

```text
/status
/audit last
/ping model
/reload profile
```

- `/status`：查看监听、连接、配置摘要、数据库计数、最近模型失败和最近审计。
- `/audit last`：查看最近 reply audits 和 system events 的脱敏摘要。
- `/ping model`：用当前模型配置发起一次最小测试请求。
- `/reload profile`：尝试重新加载角色画像；如果当前运行态不支持热替换，会提示需要重启。

### 6.3 长期记忆

```text
/memory
/memory clear
```

- `/memory`：查看当前用户长期记忆摘要。
- `/memory clear`：清空当前用户的长期记忆。

### 6.4 白名单

```text
/allow private add <qq>
/allow private remove <qq>
/allow private list
/allow group add <group_id>
/allow group remove <group_id>
/allow group list
```

这些命令会写入当前 `QQ_BOT_CONFIG_PATH` 指向的 `config.json`，并尝试热生效。

### 6.5 Root 专用 owner 管理

只有 root 可用：

```text
/owner add <qq>
/owner remove <qq>
/owner list
```

- root 在 `qq.rootUserIds` 中配置。
- root 自动拥有 owner 权限和私聊白名单权限。
- 新增 owner 不会自动加入私聊白名单、群白名单、长期记忆授权或群静默控制人。

### 6.6 群静默管理

Owner/root 私聊命令：

```text
/mute status
/mute clear
/mute clear <group_id>
```

群内控制：

- 只有 `groupMuteControllerUserIds` 里的用户可以控制。
- 在白名单群里 `@机器人 关机`、`@机器人 闭嘴`、`@机器人 退下` 等会让该群静默。
- 静默期间机器人不回复、不调用模型，但群文本仍可按配置保存到 `conversations`，低敏内容仍可更新群上下文。
- 控制人再次 `@机器人 开机`、`@机器人 说话` 或直接正常提问会解除静默并继续正常回复。

## 7. 群聊行为

群聊默认保守：

- 非白名单群沉默。
- 白名单群中 `@机器人` 可触发。
- 白名单群中命中 `qq.nicknames` 时，按 `reply.nicknameReplyProbability` 概率触发。
- 引用机器人消息并 `@机器人` 可触发线程追问。
- active window 只对同一用户明显追问生效。
- 群回复使用“引用原消息 + @提问者 + 正文”。
- 群聊最多 3 条气泡，按队列串行发送，相邻发送至少约 1.5 秒。

普通群消息不会因为刚聊过就一直让机器人接话。

## 8. 图片理解

图片理解复用当前 OpenAI-compatible 模型配置。

- 只有原本会触发回复的场景才会看图，例如 `@机器人` 带图、引用图片消息并 `@机器人`。
- 不触发的普通图片不会调用模型。
- 图片理解只发送文字回复。
- 高风险图片在群聊中默认静默，不描述图片内容。
- 如果当前模型不支持图片，会记录视觉失败并用短提示降级。

## 9. 状态工具

### 9.1 一次性检查

```bash
python tools/inspect_runtime_status.py --config config/config.json --db data/bot.db --limit 5
```

输出包含：

- QQ_bot 是否监听。
- NapCat WebUI 是否监听。
- NapCat 到 QQ_bot 是否建立连接。
- selfId、私聊白名单、群白名单。
- persona mode 和 sourceUserId。
- conversations、reply_audits、system_events、memory_profiles 计数。
- 最近审计和系统事件。
- 当前群静默状态和群队列状态。

### 9.2 持续观察

```bash
python tools/tail_bot_state.py --interval 2 --limit 5
```

只看一次：

```bash
python tools/tail_bot_state.py --once --limit 5
```

### 9.3 数据库维护

备份：

```bash
python tools/backup_db.py --db data/bot.db --backup-dir data/backups
```

导出审计：

```bash
python tools/export_audits.py --db data/bot.db --output audits.jsonl
```

VACUUM：

```bash
python tools/vacuum_db.py --db data/bot.db --backup-dir data/backups
```

## 10. Linux 运维脚本

脚本在 `scripts/server/` 下，默认项目目录为 `/opt/qq_bot`，可用环境变量覆盖：

```bash
export QQ_BOT_ROOT=/opt/qq_bot
export QQ_BOT_SERVICE=qq-bot.service
export NAPCAT_CONTAINER=napcat
```

查看状态：

```bash
bash scripts/server/status.sh
```

重启 QQ_bot 和 NapCat：

```bash
bash scripts/server/restart_all.sh
```

跟踪日志：

```bash
bash scripts/server/tail_logs.sh
```

备份运行数据库：

```bash
bash scripts/server/backup_runtime.sh
```

安装 logrotate 模板前，先按自己的项目目录修改：

```bash
sudo cp scripts/server/logrotate-qq-bot.conf /etc/logrotate.d/qq-bot
sudo logrotate -d /etc/logrotate.d/qq-bot
```

`-d` 是 dry-run，不会真正轮转日志。

## 11. 部署脚本

Windows PowerShell：

```powershell
.\scripts\deploy_server.ps1 `
  -HostName your-server-ip `
  -User your-server-user `
  -Root /opt/qq_bot
```

如果需要密码登录，不要把密码写入脚本。只放在当前 shell 环境变量：

```powershell
$env:QQBOT_SSH_PASS="your-ssh-password"
$env:QQBOT_SUDO_PASS="your-sudo-password"
.\scripts\deploy_server.ps1 -PasswordEnv QQBOT_SSH_PASS -SudoPasswordEnv QQBOT_SUDO_PASS
```

使用 SSH key 更安全。

## 12. 常见排障

### 12.1 8080 被占用

```bash
ss -ltnp | grep ':8080'
```

处理方式：

- 停掉占用端口的进程。
- 或修改 `config/config.json` 的 `onebot.port`，同时同步 NapCat 反向 WS 地址。

### 12.2 NapCat 没连上

检查：

- QQ_bot 是否在监听 `8080`。
- NapCat 反向 WS 地址是否正确。
- Token 是否一致。
- 防火墙是否允许访问。
- NapCat 是否仍保持登录。

### 12.3 模型不回复

看 `/status` 或 `tools/inspect_runtime_status.py` 的最近模型失败。

常见分类：

- `auth_error`：API Key 问题，通常是 HTTP 401/403。
- `model_or_endpoint_not_found`：模型名或接口路径问题，通常是 HTTP 404。
- `rate_limited`：限流，通常是 HTTP 429。
- `timeout`：供应商慢或网络慢。
- `provider_error`：供应商 5xx。

### 12.4 私聊不回复

检查：

- 用户是否在 `allowedPrivateUserIds` 中，或是否是 root。
- 是否命中私聊冷却或每分钟限频。
- 模型是否处于熔断状态。
- 是否是非 owner/root 发送管理命令导致静默。

### 12.5 群聊不回复

检查：

- 群是否在 `allowedGroupIds` 中。
- 是否 `@机器人` 或命中昵称概率。
- 是否处于群静默。
- 是否命中群冷却、群每分钟限频、用户每分钟限频。
- 图片或文本是否被安全策略沉默。

### 12.6 QQ 登录态掉线

常见原因：

- 同一个机器人 QQ 在其他终端登录。
- QQ 风控要求重新验证。
- NapCat 容器或数据目录被重建。
- 长期运行期间网络波动。

处理：

1. 查看 NapCat 日志。
2. 重新获取二维码并扫码登录。
3. 确认 NapCat 反向 WS 重新连接。
4. 用 `tools/inspect_runtime_status.py` 确认 `napcat_to_bot_established=true`。

## 13. 安全规则

- 不提交真实 `.env`。
- 不提交 `config/config.json`。
- 不提交 `config/persona_profile.local.json`。
- 不提交 `data/*.db`、数据库备份、日志、二维码、图片缓存或 QQ 登录态。
- 不在 README、手册、issue、commit message 中写 API Key、Token、服务器密码或真实私有账号。
- 公开仓库里的 QQ 号、群号、服务器地址均应使用占位符。

发布或推送前建议执行：

```bash
rg -n "real-password|real-token|real-server|真实服务器|真实密钥" .
```

如果命中真实信息，先清理再提交。
